Description: Fargate Service on ECS

Parameters:
  TenantId:
    Type: String
    Default: c3

  EnvId:
    Type: String
    Default: project

  ServiceName:
    Type: String
    Default: c3-api

  ContainerImageUri:
    Type: String
    Description: Container image URI

  ContainerPort:
    Type: Number
    Default: 80
    Description: Container listener port

  TaskCpu:
    Type: String
    Default: "512"
    Description: Fargate task CPU units

  TaskMemory:
    Type: String
    Default: "1024"
    Description: Fargate task memory (MiB)

  DesiredCount:
    Type: Number
    Default: 1
    Description: Desired ECS service task count

  PathPatterns:
    Type: CommaDelimitedList
    Default: "/,/*"
    Description: ALB listener rule path patterns

  HealthCheckPath:
    Type: String
    Default: /api/
    Description: ALB target group health check path

  QuarkusHttpRootPath:
    Type: String
    Default: /api
    Description: QUARKUS_HTTP_ROOT_PATH for the API container

  ListenerRulePriority:
    Type: Number
    Default: 2048
    Description: ALB listener rule priority

  C3IndexMessage:
    Type: String
    Default: Welcome to C3 API on ECS
    Description: Message returned by the API index endpoint

Resources:
  ECSAPITaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub "${EnvId}-${ServiceName}"
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      Cpu: !Ref TaskCpu
      Memory: !Ref TaskMemory
      ExecutionRoleArn:
        Fn::ImportValue: !Sub "${EnvId}-ExecutionRoleArn"
      ContainerDefinitions:
        - Name: !Ref ServiceName
          Image: !Ref ContainerImageUri
          Environment:
            - Name: C3_INDEX_MESSAGE
              Value: !Ref C3IndexMessage
            - Name: QUARKUS_HTTP_ROOT_PATH
              Value: !Ref QuarkusHttpRootPath
          PortMappings:
            - ContainerPort: !Ref ContainerPort
              Protocol: tcp
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group:
                Fn::ImportValue: !Sub "${EnvId}-AppLogGroupName"
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: ecs

  ECSAPISecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub "${EnvId}-${ServiceName} Security Group"
      VpcId:
        Fn::ImportValue: !Sub "${TenantId}-VpcId"
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: !Ref ContainerPort
          ToPort: !Ref ContainerPort
          SourceSecurityGroupId:
            Fn::ImportValue: !Sub "${EnvId}-ALBSecurityGroupId"
      SecurityGroupEgress:
        - IpProtocol: -1
          FromPort: 0
          ToPort: 0
          CidrIp: 0.0.0.0/0

  ECSAPIService:
    Type: AWS::ECS::Service
    Properties:
      ServiceName: !Sub "${EnvId}-${ServiceName}"
      Cluster:
        Fn::ImportValue: !Sub "${EnvId}-ECSClusterName"
      LaunchType: FARGATE
      DesiredCount: !Ref DesiredCount
      TaskDefinition: !Ref ECSAPITaskDefinition
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: ENABLED
          Subnets:
            - Fn::ImportValue: !Sub "${TenantId}-PublicSubnet1Id"
            - Fn::ImportValue: !Sub "${TenantId}-PublicSubnet2Id"
            - Fn::ImportValue: !Sub "${TenantId}-PublicSubnet3Id"
          SecurityGroups:
            - !Ref ECSAPISecurityGroup
      LoadBalancers:
        - ContainerName: !Ref ServiceName
          ContainerPort: !Ref ContainerPort
          TargetGroupArn: !Ref ECSAPITargetGroup
      EnableECSManagedTags: true
      PropagateTags: SERVICE

  ECSAPITargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${EnvId}-${ServiceName}"
      Port: !Ref ContainerPort
      Protocol: HTTP
      TargetType: ip
      VpcId:
        Fn::ImportValue: !Sub "${TenantId}-VpcId"
      HealthCheckProtocol: HTTP
      HealthCheckPath: !Ref HealthCheckPath
      Matcher:
        HttpCode: "200"

  ECSListenerRule2048:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: 
        Fn::ImportValue: !Sub "${EnvId}-HttpsListenerArn"
      Priority: !Ref ListenerRulePriority
      Conditions:
        - Field: path-pattern
          Values: !Ref PathPatterns
      Actions:
        - Type: forward
          TargetGroupArn: !Ref ECSAPITargetGroup

  ECSHttpListenerRule2048:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn:
        Fn::ImportValue: !Sub "${EnvId}-HttpListenerArn"
      Priority: !Ref ListenerRulePriority
      Conditions:
        - Field: path-pattern
          Values: !Ref PathPatterns
      Actions:
        - Type: forward
          TargetGroupArn: !Ref ECSAPITargetGroup

Outputs:
  ECSAPITaskDefinitionRef:
    Description: The Ref ECS Task Definition
    Value: !Ref ECSAPITaskDefinition
    Export:
      Name: !Sub "${EnvId}-${ServiceName}TaskDefinitionRef"